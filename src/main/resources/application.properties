# Configuration du port du serveur Spring Boot
# Le serveur écoutera sur le port 8080
server.port=8080

# Configuration Actuator pour les health checks
# Active l'endpoint /actuator/health pour la liveness probe
management.endpoints.web.exposure.include=health
management.endpoint.health.enabled=true

